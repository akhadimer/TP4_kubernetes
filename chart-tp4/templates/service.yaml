apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-tp4.fullname" . }}
  labels:
    {{- include "chart-tp4.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-tp4.selectorLabels" . | nindent 4 }}
